node_modules/
dist/
dist-extension/
site/dist/
.scratch/
test-results/
playwright-report/
blob-report/
*.tsbuildinfo

# Twenty-four fonts, thirty megabytes, none of them mine. `npm run fonts`
# downloads them; the manifest of what they declare is committed so the
# findings can be read without them.
test/browser/fixtures/fonts/*.ttf
test/browser/fixtures/fonts/*.woff2

# Only needed to sign a self-hosted .crx. The store issues its own id.
extension-signing-key.pem
dist-extension-test/

# Written by `node action/run.mjs` when the action is exercised locally.
quoin-results.json
.quoin-ci-baseline.json
